apiVersion: v1
kind: Service
metadata:
  name: {{ include "alwaysfail.fullname" . }}
  labels:
    {{- include "alwaysfail.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alwaysfail.selectorLabels" . | nindent 4 }}
