apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "alwaysfail.fullname" . }}-test-connection"
  labels:
    {{- include "alwaysfail.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "alwaysfail.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
