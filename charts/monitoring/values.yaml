# Default values for monitoring 
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

prometheus-mysql-exporter:
  mysql:
    host: "speed-api-mysql-0.speed.pod.cluster.local"
    user: "root"
    pass: "password"
