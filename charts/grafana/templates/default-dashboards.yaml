apiVersion: v1
kind: ConfigMap
metadata:
  name: default-dashboards
  labels:
    grafana_dashboard: "1"
data:
  mysql.json: |
{{ .Files.Get "dashboards/mysql-dashboard.json" | indent 4 }}
  node.json: |
{{ .Files.Get "dashboards/node-dashboard.json" | indent 4 }}
